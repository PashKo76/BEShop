Simple eshop built by me with blazor framework, Entity framework and postgreSQL.
Can be used in simple shops.
Now it uses AI as review manager and strongword filter (for better censor must be set).
Database haven`t web interface, must be connected via desktop client.
CSS styles was leaved for your instances.
Review page have freezes, will be fixed in future.
